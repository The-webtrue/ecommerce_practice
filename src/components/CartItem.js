import React from 'react';

const CartItem = ({item}) => {
  const {id, title, image, price, amount} = item;
  return <div>
    <div>
      {/* image */}
      <div>
          
      </div>
    </div>
  </div>;
};

export default CartItem;
